# Used when building frontend for Docker (single domain /admin path)
# Docker Compose passes these as build args — you usually don't need this file.

VITE_BASE=/admin/
VITE_WEBSITE_URL=https://yourdomain.tn
VITE_WEBSITE_ORIGIN=https://yourdomain.tn
